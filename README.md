# Sydney Property Map — V77

A browser-based interactive property map overlaying live Domain.com.au listings with planning, environmental and infrastructure data across Sydney's growth corridors. Deployed on Vercel with a Neon Postgres database for persistent pipeline and CRM storage.

---

## File Structure

```
sydney-property-map/
├── middleware.js            — Edge middleware: gates every request with session-cookie check (V74)
├── login.html               — Branded sign-in page (V74)
├── api/
│   ├── auth/
│   │   ├── login.js         — Verifies credentials, issues session cookie (V74)
│   │   ├── logout.js        — Clears session cookie (V74)
│   │   ├── me.js            — Returns current session user (V74, public endpoint)
│   │   ├── set-password.js  — Set/change contact password (admin or self) (V74)
│   │   └── update-access.js — Toggle can_login / is_admin / access_modules (admin only) (V74)
│   ├── usage/                                                                   ← NEW V76.11
│   │   ├── active.js        — Toggle is_active on a subscription. Domain: any signed-in user (Listings button uses this); TessaDEM: admin only (V76.11)
│   │   └── config.js        — Fast read of subscription config (sub_type, sub_amount, effective_start_date, is_active). Used by the dashboard for instant first paint while /api/usage loads stats in parallel (V76.11)
│   ├── usage.js             — API call usage tracking + subscription mgmt. Two backing tables: `api_subscriptions` (per-API config) and `api_usage_log` (per-call log). GET returns full stats incl. period_calls + balance_remaining; PUT updates sub fields (admin); POST logs internally. Exports `logApiCall()` and `checkApiAllowed()` for proxy short-circuiting. Auto-seeds Domain (monthly, 10000, 2026-04-27) and TessaDEM (total, 12000, 2026-05-03) on first call (V76.11)
│   ├── elevation-tile.js    — Server-side TessaDEM tile renderer. Two modes: `mode=probe` returns viewport min/max as JSON; tile mode returns a 256×256 PNG with a 19-band blue→cyan→green→yellow→orange→red→white ramp scaled to supplied min/max. Per-tile GeoTIFF fetch via TessaDEM area-mode (64×64 grid). 12-month CDN cache. Calls `checkApiAllowed('tessadem')` first — short-circuits with 503 (inactive) or 402 (quota exhausted). Logs every call to api_usage_log via `logApiCall()` (V76.10/11)
│   ├── properties.js        — Properties CRUD (V75) — permanent land identity; `state_prop_id` column added (V75.4c); `state` column added (V76.7)
│   ├── deals.js             — Deals CRUD (V75) — workflow-scoped Kanban cards; auto-cleans orphaned Parcels on DELETE (V75.4d.1). V75.6: accepts `board_id` filter in GET; accepts `board_id` + `column_id` in POST and PUT (auto-derives from legacy `workflow` + `stage` when absent, so older callers keep working). V76.2: `fetchAndExpand()` adds `has_due_action` boolean to each deal row via a single batched actions query
│   ├── roles.js             — Role catalogue CRUD (V75)
│   ├── contacts.js          — CRM Contacts CRUD, backed by entity_contacts (V75). Note endpoints return 410 Gone and redirect to /api/notes (V75.3)
│   ├── notes.js             — Unified polymorphic notes CRUD (V75.3) — replaces deals.data.notes[] and contact_notes table
│   ├── finance-api.js       — Financial model CRUD, keyed by deal_id going forward (V75)
│   ├── migrate-to-v75.js    — V75.0 structural migration endpoint (admin-only)
│   ├── migrate-to-v75-3.js  — V75.3 migration: notes unification + DD per-deal (admin-only)
│   ├── migrate-to-v75-4.js  — V75.4 migration: Parcels introduction + synthetic-row split (admin-only)
│   ├── parcels.js           — Parcels CRUD (V75.4); DELETE also removes child properties (V75.4d.1)
│   ├── repair-v75-4c.js     — V75.4c: re-queries NSW Spatial Portal at each child lat/lng to backfill authoritative addresses + `lot_dps` + `state_prop_id` (admin-only)
│   ├── rebuild-parcel-by-lotdp.js    — V75.4c: accepts client-pre-resolved lot data to rebuild a parcel's children cleanly (admin-only)
│   ├── create-parcel-from-lookup.js  — V75.4d: creates a Parcel + N Properties + Deal from client-pre-resolved NSW data (admin-only). V75.6: sets `board_id` + `column_id` on the new deal
│   ├── backfill-parcel-rings.js      — V75.4d.4: one-time backfill of lot polygon rings on child properties (admin-only)
│   ├── migrate-to-v75-6.js  — V75.6 migration (admin-only)
│   ├── migrate-to-v75-7.js  — V76.2 migration: actions table + boards.board_type column (admin-only)
│   ├── migrate-to-v76-7.js  — V76.7 migration: properties.state column (admin-only)
│   ├── domain-price-estimates.js — V76.7: cache for derived price ranges from the Reveal Price bracket-probe workflow
│   ├── boards.js            — V75.6: Boards + columns CRUD. V76.2: accepts `board_type` on POST; guards check actions refs alongside deals
│   ├── actions.js           — V76.2: Actions CRUD with server-side Due promotion + bootstrap of "My Actions" board
│   ├── deal-order.js        — V75.6: per-user card-ordering within a board column
│   ├── db-setup.js          — DB schema setup (legacy tables + auth columns)
│   ├── domain-search.js     — Domain API proxy (keeps key server-side). V76.11: short-circuits with `checkApiAllowed('domain')` — returns 503 (inactive) or 402 (quota_exhausted) before calling Domain. Logs every call (success or failure) to api_usage_log via `logApiCall()`
│   ├── tiles.js             — NSW tile proxy (query params, not path segments)
│   ├── topo-style.js        — NSW topo style proxy (CORS fix)
│   ├── health.js            — DB health check endpoint
│   ├── ── V77.1 endpoints ─────────────────────────────────────────────────────
│   ├── sources.js                  — V77.1 Contact source catalogue CRUD (lookup table for source attribution on contacts/notes)
│   ├── interaction-types.js        — V77.1 Note interaction type catalogue CRUD with `direction` (inbound/outbound/internal) — drives Note form Source-field visibility
│   ├── scheduled-inspections.js    — V77.1 Inspection events on Listing deals (date/time/type/status). Returns expanded shape with attendance count + listing context
│   ├── inspection-attendances.js   — V77.1 Inspection × Contact × Enquiry-deal links. PUT auto-creates Actions on the attendee's Enquiry deal when "Requested Follow up / Offer Form / Contract" tickboxes are flipped
│   ├── applications.js             — V77.1 Lease Offer CRUD. Multi-offer per deal (revisions, withdrawn-and-resubmitted). Status lifecycle: draft → offer_received → offer_accepted → evidence_submitted → validated. Reject/Withdrawn terminal. Accept action normalises applicants from JSONB to Contacts via `entity_contacts` and issues Step 2 token + email
│   ├── agency-agreements.js        — V77.1 Agency Agreement CRUD on Listings deals (sale or lease type, fee structure, payout trigger, term dates)
│   ├── pipeline.js                 — V77.1 Settings → Agency Agreements report data source (cross-deal report)
│   ├── enquiry-card-meta.js        — V77.1 enquiry-board kanban card metadata (contact name + interest level)
│   ├── cadastre.js                 — V77.1 NSW cadastre proxy (lot/DP polygon lookup for property modal map preview)
│   ├── migrate-to-v77.js           — V77.0 boards migration: renames legacy boards (sys_buyer_enquiry → sys_sales_enquiry, sys_agency_sales → sys_sales_listings); creates sys_lease_listings + sys_lease_enquiry; admin-only, idempotent
│   ├── migrate-to-v77-1.js         — V77.1 main schema: 9 new tables (scheduled_inspections, inspection_attendances, applications, application_evidence, application_housing_history, application_income_history, agency_agreements, contact_sources, interaction_types) + 7 contact columns + source FK rework + entity_contacts.role_id ON DELETE SET NULL + Billing board seed; admin-only
│   ├── migrate-to-v77-1b.js        — V77.1b adds deals.parent_deal_id self-reference (Listing Summary section uses this); admin-only
│   ├── migrate-to-v77-1c.js        — V77.1c drops legacy contacts.source text column + seeds `domain_api` interaction type; admin-only
│   ├── ── V77.2 endpoints ─────────────────────────────────────────────────────
│   ├── system-settings.js          — V77.2 admin-editable config table (email config, public URL). Notifications page reads/writes here
│   ├── applicant-form-tokens.js    — V77.2 magic-link token issue/verify/list/revoke for public Lease Offer forms (Step 1 + Step 2 per offer)
│   ├── public/
│   │   └── lease-offers.js         — V77.2 public token-authed endpoint for the Lease Offer form. Sub-actions via Vercel rewrite path: load (form context), verify (flip email_verified), submit-draft (in-progress save), submit (final, draft → offer_received + agent email)
│   ├── evidence-view.js            — V77.2 streams Step 2 evidence files from Vercel Blob with auth check
│   ├── evidence-download.js        — V77.2 same as evidence-view but with attachment Content-Disposition
│   ├── cleanup-tokens.js           — V77.2 daily cron (Vercel: 15:00 UTC) — hard-deletes unverified tokens older than 7 days + their orphaned draft applications. Also callable manually by admin
│   ├── migrate-to-v77-2.js         — V77.2 public-form layer: `email_log` + `system_settings` tables, applicant_form_tokens columns. Seeds system_settings with email config defaults from `lib/email-config.js`; admin-only
│   ├── migrate-to-v77-2b.js        — V77.2b adds `application_evidence` table for Step 2 file uploads; admin-only
│   ├── migrate-to-v77-2c.js        — V77.2c adds applications.retention_consent_at + lease_end_estimate; admin-only
│   ├── migrate-to-v77-2d.js        — V77.2d adds applications.validation_jsonb + `evidence_resubmit_requested` status; admin-only
│   ├── migrate-to-v77-2e.js        — V77.2e adds application_evidence.doc_type column; admin-only
│   ├── migrate-to-v77-2f.js        — V77.2f adds roles.default_for column (later replaced in 2g); admin-only
│   └── migrate-to-v77-2g.js        — V77.2g replaces roles.default_for (text) with `role_boards` join table; admin-only
├── lib/
│   ├── auth.js              — Shared JWT sign/verify, cookie helpers, session guards (V74). V77.2 adds public-token validation surface for token-authed public endpoints
│   ├── db.js                — Database URL resolver
│   ├── parcel-format.js     — `formatParcelTitle()` — collapses street numbers into ranges for Parcel display (V75.4)
│   ├── nsw-lookup.js        — Server-side NSW Spatial Portal lookup helper (V75.4c)
│   ├── email-config.js      — V77.1 hardcoded fallback email config (APP_PUBLIC_URL, sending domain, leasing/sales from-addresses, reply-to handling). Used when system_settings query fails
│   ├── email.js             — V77.2 email sending wrapper. Reads config from system_settings; falls back to email-config.js. Currently in stubbed mode (writes to email_log table; no real dispatch). V77.3+ will swap in Resend
│   ├── blob.js              — V77.2 Vercel Blob wrapper for Step 2 evidence uploads (10 MB cap per file)
│   ├── public-token-auth.js — V77.2 token-authentication middleware for `/lease-offer/{token}` page loads and `/api/public/lease-offers/{token}/{action}` endpoints
│   └── rebuild-parcel-by-lotdp.js — server-side parcel rebuild helper (companion to api/rebuild-parcel-by-lotdp.js)
├── scripts/
│   └── hash-password.mjs    — Local utility: generates bcrypt hash for fallback env var (V74)
├── finance/
│   ├── finance-module.js    — Financial feasibility calculator, UI, DB persistence
│   └── finance-styles.css   — Finance module styles
├── lease-offer/                                                                ← NEW V77.2
│   ├── index.html             — Public Step 1 form (token-authed via `/lease-offer/{token}`)
│   ├── lease-offer-form.js    — Step 1 client logic (offer terms, applicants, occupants, pets)
│   ├── lease-offer-form.css   — Step 1 styles
│   ├── step-2.html            — Public Step 2 form (token-authed via `/lease-offer/step-2/{token}`)
│   ├── step-2.js              — Step 2 client logic (100-points ID, housing/income history, evidence upload)
│   └── step-2.css             — Step 2 styles
├── emails/                                                                     ← NEW V77.2
│   ├── _layout.js             — Shared HTML/text email skeleton
│   ├── lease-offer-step-1-invite.js                — Sent when agent issues Step 1 link
│   ├── lease-offer-received-agent-notification.js  — Sent to agent after applicant submits Step 1
│   ├── lease-offer-step-2-invite.js                — Sent when agent accepts offer (Step 2 link)
│   ├── lease-offer-evidence-submitted-agent-notification.js — Sent to agent after applicant submits Step 2
│   └── lease-offer-resubmit-requested.js           — Sent when agent rejects evidence + asks for replacement
├── index.html               — Page structure and UI. V77: Settings sub-menu adds Agency Agreements, Parameters, Notifications, Retention Policy sub-pages. Lazy-renders each on first visit. V76.11: System Settings houses the API Dashboard with two cards (TessaDEM, Domain) showing subscription type/amount/effective-start, balance remaining, period calls, last call, and an active toggle. Edit Subscription modal for admin updates. Two-phase dashboard load (`/api/usage/config` first for instant paint, `/api/usage` after for stats). Quota-exhausted banner. `window._apiState` cache + `window.syncApiDependentUi()` keep listings button colour and elevation overlay row state in sync with API state.
├── styles.css               — All styling. V77: deal-modal section styles (Inspections, Agency Agreements, Lease Offer, Validation, Lease Offers Received, Listing Summary), Parameters page tables, Notifications page form, Settings sub-menu nav, Interest Level slider, Status select, V77 toolbar buttons, attendee-tickbox grids. V76.11: API Dashboard cards, active toggle, edit-subscription modal, listings-button api-inactive grey state.
├── crm.js                   — CRM contact management module. V77.1: System Settings sub-pages (Agency Agreements report, Parameters page with Roles/Sources/Interaction Types tabs), Notifications page (V77.2), Retention Policy page (V77.2c). Removes hardcoded ROLES array — reads from cached `/api/roles` + `/api/sources` + `/api/interaction-types` (see `lookups.js`). `window.openContactModal` (CRM-view-scoped) and `window.openContactModalStandalone` (V77.2g+, body-mounted for use from kanban deal modals where `#crmView` is hidden). Note form extended with Type + Source dropdowns at every callsite.
├── crm-styles.css           — CRM-specific styles. V77: `.crm-note-text` adds `white-space: pre-wrap` so multi-line notes render with linebreaks preserved.
├── lookups.js                  — V77.1 client-side cache for /api/roles, /api/sources, /api/interaction-types with cross-tab BroadcastChannel invalidation
├── note-form.js                — V77.1 reusable Note form with interaction_type + source dropdowns (used by every note callsite across the app)
├── inspections-section.js      — V77.1 Inspection Schedule + Register Attendees sub-modal (Listings only)
├── agency-agreements-section.js — V77.1 Agency Agreements section on Listings deal modal
├── agency-agreements-report.js — V77.1 Settings → Agency Agreements report
├── lease-offer-section.js      — V77.1 Lease Offers section on Lease Enquiry deal (multi-offer list, "+ New Lease Offer", Step 1+2 fields, Accept/Reject)
├── lease-offers-received-section.js — V77.1 Lease Offers Received cross-reference on Lease Listings deal (read-mostly)
├── validation-section.js       — V77.1 Lease Offer evidence review + per-item Validated checkboxes + 100-points ID
├── listing-summary-section.js  — V77.1b read-only listing terms display on Enquiry deals (parent_deal_id-based)
├── kanban-new-card.js          — V77.1 "+ New Card" toolbar button: single-step (Listings) or two-step (Enquiry: property → contact + first-inbound-note context). V77.3: optional "Enquiry message" textarea on the contact step → first inbound note prefixed with "Enquiry:" line and the captured message.
├── parameters-page.js          — V77.1 Settings → Parameters page (Roles / Sources / Interaction Types tables + edit modals)
├── notifications-page.js       — V77.2 Settings → Notifications page (Email Configuration; placeholders for V78 Subscriptions/Event Types)
├── retention-policy-page.js    — V77.2c Settings → Retention Policy page (document retention period + pending purges)
├── overlays-meta.js         — Overlay definitions, zone config, type metadata. V76.11: adds `tessadem-elevation` overlay; `OVERLAY_TYPE_META` adds `'elevation'` (label "Topographic")
├── overlays-b64-sw-wastewater.js  — SW Sydney wastewater GeoTIFF (b64)
├── overlays-b64-sw-potable.js     — SW Sydney potable water GeoTIFF (b64)
├── overlays-b64-sw-ilp.js         — Leppington ILP GeoTIFF (b64)
├── gsp-wsa-sw-wastewater.js       — WSA wastewater GeoJSON (planning stages)
├── WSA_SW_Wastewater_Precincts.geojson
├── catherine_park_north_zoning_wgs84.geojson
├── domain-api.js            — Domain API client (V76.7 enhancements)
├── dd-risks.js              — DD risk assessment (queries NSW layers at lat/lng)
├── nsw-lookup-client.js     — Browser-side NSW Spatial Portal helper (V75.4c/d)
├── map.js                   — Map logic, overlays, search, listings, Domain init. V76.11: `tessadem-elevation` viewport-adaptive layer.
├── kanban.js                — Pipeline Kanban board. V77: board-aware deal modal that mounts board-specific sections (Inspections + Agency Agreements on Listings; Lease Offer + Validation on Lease Enquiry; Lease Offers Received on Lease Listings; Listing Summary + Interest Level slider + Status select on Enquiry). V77 also adds the "+ New Card" toolbar button (board-aware via `kanban-new-card.js`).
├── lib/
│   └── ... (see top of file structure)
├── api/
│   ├── ... (see top of file structure for V77.1+V77.2 endpoint additions)
├── package.json             — Dependencies. V77.2 adds `@vercel/blob` (Step 2 evidence file uploads) and `resend` (email sending — wired in stubbed mode for V77.2; live for V77.3+). V76.11 added `geotiff` and `sharp`.
├── vercel.json              — Vercel routing config. V77.2 adds rewrites for `/lease-offer/{token}` (Step 1) and `/lease-offer/step-2/{token}` (Step 2), plus public API rewrites and a Cron entry for `api/cleanup-tokens` (daily 15:00 UTC).
├── DEPLOY.md                — Deployment guide
└── README.md                — This file
```

> **Note:** `data.js` has been removed. Listings come exclusively from the live Domain API.

---

## Environment Variables (Vercel)
| Variable | Description |
|---|---|
| `DOMAIN_API_KEY` | Domain Developer API key — store in Vercel only, never in code |
| `TESSADEM_API_KEY` | TessaDEM Elevation API key — used by `api/elevation-tile.js` for the Elevation Gradient overlay (V76.11) |
| `POSTGRES_URL` | Neon database connection string (auto-injected by Vercel) |
| `JWT_SECRET` | Session JWT signing secret — minimum 32 random chars. Generate: `openssl rand -base64 48` |
| `ADMIN_FALLBACK_EMAIL` | Break-glass admin email (e.g. `alan.diversi@edanproperty.com.au`). Always has admin access, works even if DB is unreachable |
| `ADMIN_FALLBACK_PASSWORD_HASH` | Bcrypt hash of the fallback admin password. Generate locally with `node scripts/hash-password.mjs` |
| `BLOB_READ_WRITE_TOKEN` | Vercel Blob read-write token. Created in Vercel → Storage → Blob. Used by `api/public/lease-offers` Step 2 evidence uploads + `api/evidence-view`/`api/evidence-download` (V77.2) |
| `CRON_SECRET` | Random 32+ char secret. Generate: `openssl rand -base64 48`. Used by `api/cleanup-tokens` to verify daily Vercel Cron requests are legit (V77.2) |
| `RESEND_API_KEY` | Resend API key for outgoing emails. **Optional in V77.2** — `lib/email.js` runs in stubbed mode (writes to `email_log` table, no real dispatch) when not set. Required when V77.3+ swaps to live email |

---

## Version History

| Version | Notes |
|---|---|
| V77 | **Sales/Lease pipeline release.** Single prod release composed of V77.0 (boards) + V77.1 (agent-side workflow + schema) + V77.2 (public-form layer) + V77.3 fixes from preview. **(1) Five system kanban boards instead of two**: `sys_acquisition` (existing, renamed from internal id), `sys_sales_listings`, `sys_sales_enquiry`, `sys_lease_listings`, `sys_lease_enquiry`, plus an empty `sys_billing` placeholder for a later release. Existing Acquisition deals are unchanged. **(2) Deal modal sections are board-aware**: Listings deals get Inspection Schedule + Agency Agreements; Enquiry deals get Listing Summary (read-only parent terms) + Interest Level slider + Status select; Lease Enquiry adds Lease Offers + Validation; Lease Listings adds Lease Offers Received (cross-reference, read-mostly). Every section is its own client module and a first-class object on the server (no JSONB blobs except where parked: Sales Offers stays JSONB on Acquisition until the Acquisition data migration is in scope). **(3) Inspections are first-class**: Schedule sub-modal on Listings, Register Attendees sub-modal links existing Contacts to Enquiry deals (auto-creates Enquiry deals when needed), per-attendee tickboxes for Requested Follow up / Offer Form / Contract auto-create Actions on the Enquiry deal assigned to the Listing Agent. Marketing-consent tickboxes write through to the contact (`marketing_email_consent_at`, `marketing_sms_consent_at`, `do_not_contact_at`). **(4) Lease Offer is two-phase**: Phase 1 = offer terms (rent, bond, term, applicants, occupants, pets, special terms, claimed income); Phase 2 = evidence (100-points ID, housing history, income history). Status lifecycle: `draft → offer_received → offer_accepted → evidence_submitted → validated`. Reject/Withdrawn terminal. Multi-offer per Lease Enquiry deal supported (negotiation revisions; withdrawn-then-resubmitted). Applicants link via `entity_contacts` with role `applicant` / `co_applicant` (Option A — no separate join table). **(5) Public-form layer (V77.2)**: per-offer magic-link tokens (table `applicant_form_tokens`); applicants reach `https://propmap.edanproperty.com.au/lease-offer/{token}` (Step 1) or `/lease-offer/step-2/{token}` (Step 2 — gated on agent Accept). Email verification on first click extends token to 30 days; unverified tokens expire after 7 days and are reaped daily by `api/cleanup-tokens` cron. Agent UI exposes Copy / Resend / Reissue controls per token (§6.1.2a). Step 2 evidence uploads via Vercel Blob (10 MB cap per file). Email infrastructure is **stubbed** in V77.2 — every send writes to `email_log` with `status='stubbed'` instead of dispatching; V77.3+ wires Resend. **(6) Notes table extended** instead of separate enquiry_interactions table — adds `interaction_type` (FK to `interaction_types`) and `source` (FK to `contact_sources`); existing notes backfilled to `interaction_type='file_note'`. The Note form everywhere reads Type and Source dropdowns from the cached `/api/interaction-types` and `/api/sources` lookup tables. The "+ New Card" two-step Enquiry flow captures the enquirer's actual message in an optional textarea on the contact step → first inbound note is created with the system summary + an "Enquiry:" labelled block holding the message. **(7) Agency Agreements** are first-class on Listings deals (sale or lease type, fee structure, payout-trigger dropdown matched to type, term dates). Cross-deal report at System Settings → Agency Agreements with sortable columns and "Open deal" jump-to-board behaviour. **(8) System Settings reorganised** alphabetically: Agency Agreements / API Dashboard / Notifications / Parameters / Retention Policy. Parameters page houses Roles / Sources / Interaction Types lookup tables with sortable columns + system-row protection (deactivate-only) + reference-count warnings on delete. Notifications page configures email-sending domain, leasing/sales from-addresses, public form base URL (`system_settings` table — V77.2; `lib/email-config.js` is the fallback). **(9) "+ New Card" toolbar** on V77 system boards: single-step on Listings (property picker → deal in Prospecting); two-step on Enquiry (property → contact → deal in Enquiry, with `entity_contacts` role='enquirer' and the optional Enquiry message captured into a first inbound note). The "+ Pipeline" button on map listing pins still creates Acquisition deals as before. The CRM property modal "+ New Deal" buttons are removed. **(10) Granular contact preferences**: independent timestamps for `privacy_consent_at`, `marketing_email_consent_at`, `marketing_sms_consent_at`, `do_not_contact_at`. Plus `dob`, `current_address`+suburb/state/postcode columns. **(11) "+ Create new contact" sub-modal** in deal-modal Add Contact flow now uses a body-mounted overlay (`window.openContactModalStandalone`) so it works when the CRM view is hidden (i.e. when called from a kanban deal modal). The CRM-view-scoped variant is kept for in-CRM callsites. **New deps**: `@vercel/blob` (Step 2 file uploads), `resend` (email — used in stubbed mode initially). **New env vars**: `BLOB_READ_WRITE_TOKEN`, `CRON_SECRET`, `RESEND_API_KEY` (V77.3+ only). **DB migrations** — run in order, all admin-only POST, all idempotent: `migrate-to-v77` (boards rename + Lease boards seed), `migrate-to-v77-1` (main schema: 9 tables + contact columns + notes extension + source FK rework + entity_contacts.role_id ON DELETE SET NULL + Billing board), `migrate-to-v77-1b` (deals.parent_deal_id), `migrate-to-v77-1c` (drop legacy contacts.source + seed domain_api type), `migrate-to-v77-2` (email_log + system_settings + token columns), `migrate-to-v77-2b` (application_evidence), `migrate-to-v77-2c` (retention_consent_at + lease_end_estimate), `migrate-to-v77-2d` (validation_jsonb + evidence_resubmit_requested), `migrate-to-v77-2e` (evidence.doc_type), `migrate-to-v77-2f` (roles.default_for — superseded), `migrate-to-v77-2g` (role_boards join table replacing default_for). **Vercel Cron**: `api/cleanup-tokens` daily 15:00 UTC (≈02:00 AEDT) — discards unverified tokens older than 7 days plus their orphaned draft applications. **Rollback**: V77 schema is additive — old code reading the V76 schema still works against a V77 DB. To hard-rollback DB, drop new tables in reverse FK order (ask Claude for the script). For prod-deploy runbook see `V77_PROD_DEPLOY.md`. |
| V76.12b | **Two kanban bug fixes shipped together.** **(1) Deal-delete is now deal-only.** Fixed a long-standing bug where deleting a deal card on a property that had multiple deals would wipe the property record and cascade-delete every other deal on it (because the client routed property-deal deletes to `DELETE /api/properties` and `deals.property_id` has `ON DELETE CASCADE`). New behaviour: `DELETE /api/deals?id=X` removes only the deal row plus its deal-scoped data — `property_financials` (by `deal_id`), `entity_contacts` where `entity_type='deal'`, `notes` where `entity_type='deal'`, `actions` where `deal_id=<id>`, and `deal_user_order` (cascades via FK). Properties, parcels, child properties, and any property-scoped contacts/notes are explicitly NOT touched. Property/parcel deletion remains a CRM-only operation (the CRM already client-side-disables the delete button when deals reference the entity, so the existing safe workflow is preserved). Confirm modal wording updated: "Are you sure you want to delete this card? The property record and any associated contacts, notes or other property-level data will not be affected." Address still shown as the modal subject. **Behaviour change to flag**: the V75.4d auto-orphan-cleanup of parcels (server would auto-delete a parcel + child properties when the last deal on it was removed) is GONE. A parcel that ends up with zero deals now stays — it's cleaned up from the CRM where contact/note implications are visible. **(2) Kanban board repaints when the deal card modal closes.** Fixes a stale-board bug most visible after creating a new deal from the CRM property modal's "+ New Deal" button: the deal was created, the modal opened, and the background board reload populated the in-memory pipeline dict — but the actual board paint was deliberately suppressed while the modal was open (to avoid yanking the page mid-edit), and nothing repainted on close. Result: the new card stayed invisible until the user did something else that triggered a render (board switch, search edit, etc.). The same suppression hid in-modal edits — e.g. add an offer in the modal, the offer count badge on the card behind it would be stale until the next interaction. **Fix**: in `openCardModal`, the three explicit-close paths (X button, click outside, Escape key) now go through a `closeAndRefresh` helper that removes the overlay AND calls `renderBoard()` if the kanban is visible. The Finance-picker transition path is intentionally NOT changed — the user is leaving the view. The delete-from-modal path is also untouched — `removeFromPipeline` already calls `renderBoard` itself. **Files touched**: `api/deals.js` (DELETE handler rewritten — deal-only with explicit cascade list, fail-soft on missing tables), `kanban.js` (`dbDelete` simplified to always route to `/api/deals`, `removeFromPipeline` no longer captures `_isParcel`/propertyId for routing, `openDeleteCardConfirm` reworded, `openCardModal` close handlers wrapped in `closeAndRefresh`). **No DB schema change. No migration. No API contract change for callers that don't read response counters.** Older clients calling `DELETE /api/deals?id=X` get the safer new behaviour transparently. Response shape adds `financials_deleted`, `contact_links_deleted`, `notes_deleted`, `actions_deleted` counters; preserves `property_deleted`/`parcel_deleted`/`properties_deleted` keys (always false/0 in V76.12b) for any consumers reading them. **Deploy sequence**: push files → Vercel auto-deploys. **Verify**: (a) on a property with two deals (e.g. Shortlisted + Acquired), deleting one card only removes that card; the property and the other card stay; (b) deleting a parcel-deal removes only the deal, not the parcel or child properties; (c) the deal modal's Delete button shows the new wording; (d) "+ New Deal" from CRM property modal → fill in deal modal → close → new card appears on kanban board (with or without an active search filter, provided it matches); (e) edit a deal's offers/DD/notes inside the modal → close → card visuals update immediately. **Rollback**: revert `api/deals.js` and `kanban.js`. |
| V76.11 | **API Dashboard, subscription model, elevation gradient overlay.** Three connected feature areas bundled as one release. **(1) TessaDEM Elevation Gradient overlay** — new "Elevation Gradient" overlay in the Environmental group (id `tessadem-elevation`). Server-side `api/elevation-tile.js` fetches GeoTIFFs from TessaDEM (area mode, 64×64 grid per tile), decodes with `geotiff`, renders coloured PNG with `sharp`. 19 discrete colour bands: blue → cyan → green → yellow → orange → red → white. Range scales to current viewport min/max via a separate `mode=probe` endpoint that returns `{min, max}` JSON for the viewport bbox. Client-side: `map.js` overrides `getTileUrl` to inject current min/max from local state into each tile URL; debounced 400ms `moveend` triggers a fresh probe; tiles return a transparent placeholder until first probe completes (no error spam during initial load); aborts in-flight probes when a new viewport supersedes them. CDN-cached for 12 months on success — Sydney basin browsing builds up a useful tile cache after first visit. **(2) Subscription + usage tracking model**. New `api_subscriptions` table (one row per API: `api_name PK, sub_type 'monthly'|'total', sub_amount INT, effective_start_date DATE, is_active BOOL, updated_at, updated_by`). Auto-seeded on first call: Domain (monthly, 10000, 2026-04-27, active), TessaDEM (total, 12000, 2026-05-03, active). New `api_usage_log` table (one row per upstream call: api_name, called_at, status_code, balance_remaining, metadata JSONB). New endpoints: `GET /api/usage/config` (fast — single SELECT, no period math), `GET /api/usage` (full stats incl. period_calls + balance_remaining via period math), `PUT /api/usage` (admin updates sub_type/sub_amount/effective_start), `POST /api/usage/active` (Domain: any signed-in user, TessaDEM: admin only), internal `POST /api/usage` (call log, used by proxies). Period math: monthly subs roll on the same day-of-month as effective_start_date (e.g. 27 Apr → 27 May → 27 Jun). Total subs run from effective_start_date to forever. **`checkApiAllowed()`** helper exported from `api/usage.js`: looked up by `api/elevation-tile.js` and `api/domain-search.js` before every upstream call — returns 503 if `is_active=false`, 402 if `balance_remaining ≤ 0`, never letting the proxy hit the upstream when blocked. **(3) System Settings → API Dashboard** (admin-only menu item already existed as empty scaffold from V75.2). Two cards (TessaDEM, Domain) each showing: green/grey active toggle in the header, sub-type, sub-amount, effective_start_date, balance_remaining, period_calls (with period label e.g. "(27 Apr → 27 May)" for monthly), last_call_at, edit-subscription button, and (TessaDEM only) "Top up at TessaDEM" external link. **Two-phase dashboard load**: opens with `/api/usage/config` for instant paint of toggle + sub fields, then fills balance/period/last-call fields when the slower `/api/usage` returns — first-paint perceived latency dropped from ~1s to ~150ms. Edit modal lets admin change type, amount, and effective start date in one go (top-up flow). **Listings button cross-link**: clicking the existing Listings button in the listings sidebar header now also POSTs to `/api/usage/active` to toggle Domain `is_active` — green dot when active, grey when inactive. Any signed-in user can use this (deliberate exception to admin-only rule on the dashboard toggle). **Elevation overlay greying**: when TessaDEM `is_active=false`, the Elevation Gradient overlay row in the Overlays panel greys out, the checkbox disables, and "(API off)" appears next to the label. **Quota-exhausted banner**: when `api/elevation-tile` returns 402, the elevation layer auto-disables client-side and a top-of-map banner appears. For admins: "Elevation quota exhausted — Settings" with Settings as a clickable link to the dashboard. For non-admins: "Elevation quota exhausted — please contact administrator" (no link). **Architecture detail — Vercel filesystem routing**: `/api/usage/active` and `/api/usage/config` had to be split into `api/usage/active.js` and `api/usage/config.js` because Vercel's serverless routing maps URL paths to files; a path-detecting `if (url.includes('/active'))` branch in a single `api/usage.js` returns 404 because the file doesn't exist at that path. **New deps**: `geotiff` (^2.1.3), `sharp` (^0.33.5). **New env var**: `TESSADEM_API_KEY`. **No DB migration needed** — both new tables (`api_subscriptions`, `api_usage_log`) are auto-created on first endpoint call (idempotent CREATE TABLE IF NOT EXISTS). Default subscription rows seeded by `INSERT … ON CONFLICT DO NOTHING`. **Deploy sequence**: push files → set `TESSADEM_API_KEY` in Vercel → redeploy (so the new deps are installed) → verify (a) Elevation Gradient toggleable in Overlays panel and renders a banded coloured ramp, (b) Settings → API Dashboard shows both cards with subscription details, (c) editing a subscription persists across reload, (d) flipping Domain off via dashboard greys the Listings button and clicking the Listings button flips it back on, (e) flipping TessaDEM off greys the elevation overlay row. Rollback: `DROP TABLE api_usage_log; DROP TABLE api_subscriptions;` — both additive, no other tables touched. The two new overlay/proxy files (`api/elevation-tile.js`, `api/usage*.js`) and the dashboard chrome can be removed without affecting any V76.7 or earlier behaviour. |
| V76.7 | **State field, Reveal Price, Listed Since filter, suburb-duplication fix, cross-module change broadcast.** *(see prior notes — unchanged)* |
| V76.2 | **Actions — assignable tasks with their own Kanban wall.** *(see prior notes — unchanged)* |
| V76.1 | **Promotion to production of the V75.5 + V75.6 feature stack.** *(see prior notes — unchanged)* |
| V75.6.x and earlier | *(See prior README revisions for V75.x and V74.x history.)* |

