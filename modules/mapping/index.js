/**
 * modules/mapping — Map, Domain listings, overlays, NSW lookup.
 * Security: access_modules includes "mapping".
 * Scripts (load order in index.html): overlays → domain-api → nsw-lookup → map.js
 */
